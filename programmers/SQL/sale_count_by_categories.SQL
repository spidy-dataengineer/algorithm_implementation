SELECT A.CATEGORY,
       sum(B.SALES) AS TOTAL_SALES
FROM BOOK A
INNER JOIN (
            SELECT *
            FROM BOOK_SALES
            WHERE SALES_DATE BETWEEN '2022-01-01' AND '2022-01-31'
            ) B
ON A.BOOK_ID = B.BOOK_ID
GROUP BY A.CATEGORY
ORDER BY A.CATEGORY